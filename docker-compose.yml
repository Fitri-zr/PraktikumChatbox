version: "3"

services:
  server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GEMINI_API_KEY=AIzaSyD8WtmV1d2mBNryqlnBRqno08IyxPmtpwg

  client:
    image: nginx
    ports:
      - "3000:80"
    volumes:
      - ./client:/usr/share/nginx/html
    depends_on:
      - server